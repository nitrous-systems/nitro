//! Placeholder: the parent session replaces this file with the real
//! binary (it opens the window and runs the loop). It exists only
//! because cargo refuses to resolve the `[[bin]]` target without it.

fn main() {}
